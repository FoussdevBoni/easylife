
  import { useMemo } from 'react';
  
  const ROUTES = {
    DASHBOARD: '',
    PRODUCTS: 'products',
    ORDERS: 'commandes',
    PHARMACIES: 'prestataires',
  };
  
  // Définition des menus pour chaque layout
   const navigation = useMemo(() => , []);
  
  